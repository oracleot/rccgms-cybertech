/**
 * OBS Browser Source — /bible/obs
 *
 * Add as a Browser Source at any size; the passage sizes itself to fit.
 * Transparent by default. Appearance is set from the OBS dock's gear panel.
 * /bible/obs/scene is the same surface under its original name.
 */

import { BibleObsSurface } from "@/components/bible/obs-surface"

export default function BibleObsPage() {
  return <BibleObsSurface />
}

/**
 * OBS Browser Source — /bible/obs/scene
 *
 * Kept so existing OBS setups keep working; renders the same surface as /bible/obs.
 */

import { BibleObsSurface } from "@/components/bible/obs-surface"

export default function BibleObsScenePage() {
  return <BibleObsSurface />
}
